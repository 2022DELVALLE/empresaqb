import React from 'react';
import ReactDOM from 'react-dom';

function ModalAddCart () {
    return ReactDOM.createPortal(
    <p></p>, document.getElementById('modaladdcart')
    )
};

export default ModalAddCart;